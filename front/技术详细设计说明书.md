# 微信购物商城小程序 - 前端技术详细设计说明书

**版本：** v1.1  
**日期：** 2026-04-14  
**作者：** 前端开发 - 小前  
**状态：** 技术设计稿  
**分支：** test

---

## 1. 技术架构概述

### 1.1 技术栈选型

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| **框架** | UniApp Vue3 | 跨平台小程序（微信/支付宝等），Vue3 组合式 API |
| **状态管理** | Pinia | 轻量级状态管理，支持持久化 |
| **HTTP** | uRequest (拦截器封装) | 统一请求封装、token 管理、错误处理 |
| **UI 组件** | uView UI + 自定义组件 | 基础组件 + 业务组件 |
| **路由** | UniApp Router |pages.json 配置式路由 |
| **构建** | Vite + HBuilderX | 开发热更新，生产打包 |

### 1.2 项目结构

```
wechat-miniapp/
├── pages/                    # 页面目录
│   ├── index/                # 首页
│   ├── category/             # 分类页
│   ├── cart/                 # 购物车
│   ├── user/                 # 我的
│   ├── search/               # 搜索
│   ├── detail/               # 商品详情
│   ├── confirm/              # 订单确认
│   ├── orders/               # 订单列表
│   ├── order-detail/         # 订单详情
│   ├── address/              # 地址管理
│   └── address-edit/         # 地址编辑
├── components/               # 公共组件
│   ├── GoodsCard/            # 商品卡片
│   ├── CartItem/             # 购物车项
│   ├── RegionPicker/         # 区域选择器
│   ├── AddressCard/          # 地址卡片
│   └── OrderCard/            # 订单卡片
├── store/                    # Pinia store
│   ├── user.js               # 用户状态
│   ├── cart.js               # 购物车状态
│   ├── region.js             # 区域状态
│   └── order.js              # 订单状态
├── api/                      # 接口层
│   ├── index.js              # 接口统一出口
│   ├── goods.js              # 商品接口
│   ├── order.js             # 订单接口
│   └── user.js              # 用户接口
├── utils/                     # 工具函数
│   ├── request.js           # 请求封装
│   ├── storage.js          # 持久化封装
│   ├── format.js           # 格式化工具
│   └── const.js            # 常量定义
├── static/                    # 静态资源
│   ├── images/             # 图片资源
│   └── icons/              # 图标资源
├── App.vue                   # 根组件
├── main.js                   # 入口文件
├── pages.json                # 页面配置
├── manifest.json             # 应用配置
└── uni.scss                   # 全局样式变量
```

### 1.3 设计规范

#### 1.3.1 色彩体系

> 基于精品电商风格定调：**简洁高品质 + 适度的促销氛围**

| 用途 | 颜色名称 | 色值 | 使用场景 |
|------|----------|------|----------|
| **主色** | 活力红 | `#E4393C` | 按钮、重要价格、标签 |
| **主色浅** | 珊瑚红 | `#FF6B6B` | Banner 渐变、hover 态 |
| **辅色** | 深空灰 | `#111111` | 标题、重要文字 |
| **正文** | 炭黑 | `#333333` | 正文文字 |
| **次要** | 中灰 | `#666666` | 副标题、描述 |
| **辅助** | 浅灰 | `#999999` | 提示文字、划线价 |
| **边框** | 边框灰 | `#EEEEEE` | 分割线、边框 |
| **背景** | 背景灰 | `#F5F5F5` | 页面背景 |
| **白色** | 纯白 | `#FFFFFF` | 卡片、弹窗背景 |
| **成功** | 翠绿 | `#11998E` | 上架状态、已完成 |
| **警告** | 橙色 | `#F2994A` | 库存预警 |
| **失效** | 灰色 | `#CCCCCC` | 下架、禁用状态 |

#### 1.3.2 字体规范

| 用途 | 字号 (px) | 行高 | 字重 | 场景 |
|------|----------|------|------|------|
| 大标题 | 17 | 1.3 | 700 | Banner 标题 |
| 页面标题 | 16 | 1.3 | 600 | 页面顶bar |
| 卡片标题 | 14 | 1.35 | 600 | 商品名称 |
| 正文 | 13 | 1.45 | 400 | 描述文字 |
| 小字 | 12 | 1.4 | 400 | 辅助信息 |
| 微字 | 11 | 1.35 | 400 | 标签、小字 |

#### 1.3.3 间距体系 (rpx)

| 名称 | 取值 | 使用场景 |
|------|------|----------|
| xs | 4 | 标签内边距 |
| sm | 8 | 紧凑间距 |
| md | 12 | 常规间距 |
| lg | 16 | 区块间距 |
| xl | 20 | 大区块间距 |
| xxl | 24 | 页面边距 |

#### 1.3.4 圆角规范

| 名称 | 取值 (rpx) | 使用场景 |
|------|-----------|----------|
| sm | 4 | 按钮、小标签 |
| md | 8 | 输入框、小卡片 |
| lg | 12 | 商品卡片 |
| xl | 14 | 大卡片、Banner |
| full | 9999 | 胶囊按钮、Pill |

---

## 2. 核心模块详细设计

### 2.0.0 接口与领域口径（与 PRD v1.3 / 后端设计 V1.1 / 高保真原型对齐）

- **基础路径：** 后端统一 `baseURL=/api/v1`（下文接口均以此为前缀）。  
- **区域标识：** 前端/接口统一使用 `region_code`（如 `EC/NC/SC/NE`）。  
- **分类标识：** 对后端请求使用 `category_id`；UI 展示使用 `category.name`；“全部”用 `category_id` 不传或传空串表示。  
  - 说明：分类列表从 `GET /api/v1/categories` 获取，前端在 Store 中维护 `{ id, name }` 映射；首页 Pill/分类页展示用 `name`，请求筛选一律传 `id`。  
- **首页：** 主推/列表截取等规则以服务端返回为准；前端不重复实现排序/截断逻辑，避免前后不一致。

### 2.0 区域模块

#### 2.0.1 区域选择器组件

```
组件: RegionPicker
位置: 首页顶部 Bar
触发: 点击区域名称展开
动画: 上滑淡入 (200ms ease-out)
```

**UI 规格：**
- 弹窗背景：`rgba(0,0,0,0.45)`
- 选项卡片：圆角 12px，白色背景
- 选中态：活力红边框 + 浅红背景 (`#FFECEB`)
- 区域选项布局：2 列宫格

**数据结构：**
```javascript
interface Region {
  code: string;      // region_code: 'EC', 'NC', 'SC', 'NE'
  name: string;      // '华东', '华北', '华南', '东北'
  enabled: boolean;  // 是否启用
}
```

**持久化：**
- 缓存 key: `demo_region`
- 首次进入：弹窗选择（无缓存时）
- 切换：实时更新并刷新首页

#### 2.0.2 区域状态管理 (Pinia)

```javascript
// store/region.js
export const useRegionStore = defineStore('region', {
  state: () => ({
    currentRegion: storage.get('demo_region') || 'EC',
    regionList: []
  }),
  getters: {
    regionName: (state) => {
      const r = state.regionList.find(i => i.code === state.currentRegion);
      return r?.name || '华东';
    }
  },
  actions: {
    setRegion(region_code) {
      this.currentRegion = region_code;
      storage.set('demo_region', region_code);
    }
  }
});
```

---

### 2.1 首页模块

#### 2.1.1 页面结构

```
┌────────────────────────────────┐
│ 顶部 Bar (区域 + 搜索入口)        │ 44px
├────────────────────────────────┤
│ Banner 轮播                    │ 200px
├────────────────────────────────┤
│ 分类横滑 Pill                  │ 44px
├────────────────────────────────┤
│ 主推大卡                       │ 118px
├────────────────────────────────┤
│ 商品列表 (3列)                 │ 自适应
└────────────────────────────────┘
```

#### 2.1.2 Banner 轮播组件

```
组件: HomeBanner
规格:
  - 高度: 200rpx (响应式可调整)
  - 自动轮播: 3s 间隔
  - 指示器: 底部圆点 6px
  - 点击跳转: 商品详情/分类页/搜索结果
```

**Banner 数据结构：**
```javascript
interface Banner {
  id: string;
  kicker: string;          // 角标
  title: string;          // 主标题
  subtitle: string;      // 副标题
  promo: string;         // 促销行 (橙色)
  cta: string;          // 按钮文案
  image: string;        // 背景图 URL
  bg: string;          // 渐变背景 (无图时)
  goto: {
    type: 'product' | 'category' | 'search';
    productId?: string;
    category?: string;
    keyword?: string;
  };
}
```

**UI 细节：**
- 图片模式：背景图 + 底部半透明渐变层 + 文字叠加
- 纯色模式：CSS 渐变背景 + 文字
- 标题阴影：`text-shadow: 0 1px 8px rgba(0,0,0,0.35)`
- CTA 按钮：`backdrop-filter: blur(4px)` + 半透明背景

#### 2.1.3 分类横向筛选 Pill

```
组件: HomeCategoryPill
布局: 横向滚动容器
单条: padding 8px 14px, 圆角 9999px (胶囊)
间距: 8px gap
选中态: 黑色背景 #111, 白色文字
默认态: 白色背景 #FFF, 边框 #EEE
```

**数据结构：**
```javascript
interface Category {
  id: string;
  name: string;         // '农产品' / '生鲜水果' / ...
  enabled: boolean;
}
```

**行为：**
- 固定「全部」在首位
- 点击筛选：仅影响首页内商品列表（不切换 Tab）
- 切换后刷新主推 + 列表数据

#### 2.1.4 主推卡片 (Featured)

```
组件: HomeFeaturedCard
尺寸: 撑满宽度 351rpx, 高度 118rpx
布局: 左侧图 118rpx + 右侧内容区
圆角: 14rpx
阴影: 0 2px 12px rgba(0,0,0,0.06)
```

**UI 元素：**
| 元素 | 样式 |
|------|------|
| 标签 | 10px, 活力红边框, 圆角 4px |
| 名称 | 14px, 字重 700, 最多 2 行 |
| 描述 | 11px, 灰色, 最多 1 行截断 |
| 现价 | 18px, 字重 800, 活力红 |
| 划线价 | 12px, 灰色, 删除线 |
| 按钮 | 胶囊按钮, 灰背景 |

#### 2.1.5 商品列表 (3列宫格)

```
组件: HomeGoodsGrid
布局: CSS Grid, 3 列, 8px gap
列宽: (351 - 16) / 3 ≈ 111rpx
单卡结构:
  ┌────────────┐
  │ 1:1 图片  │  (商品图)
  ├────────────┤
  │ 标签     │  (营销标签,可选)
  │ 名称     │  (2行省略)
  │ 促销    │  (促销文案,可选)
  │ ¥XX   │ ¥XX  │  (价格 + 购物车)
  └────────────┘
```

**UI 细节：**
| 元素 | 样式 |
|------|------|
| 卡背景 | 白色, 圆角 12rpx |
| 阴影 | 0 1px 6px rgba(0,0,0,0.06) |
| 图片比例 | 1:1 (正方形) |
| 购物车按钮 | 22px 圆形, 浅红背景 (#FFECEB) |
| 价格 | 13px, 字重 800, 活力红 |
| 划线价 | 9px, 灰色, 删除线 |

**加载更多：**
- 方案：底部「加载更多」按钮
- 状态：加载中 / 有更多 / 没有更多
- 点击加载：追加下一页数据

#### 2.1.6 首页数据获取流程

```javascript
// API 调用流程
1. GET /api/v1/home/banners?region_code={regionCode}
2. GET /api/v1/home/featured?region_code={regionCode}&category_id={categoryId?}
3. GET /api/v1/home/products?region_code={regionCode}&category_id={categoryId?}

说明:
- 首页候选池过滤、销量排序、主推自动/指定与兜底、列表 N 截取与 clamp 均由服务端完成
- 前端按 featured + products 渲染(主推与列表互斥，服务端保证不重复)
```

---

### 2.2 分类页模块

#### 2.2.1 页面结构

```
┌────────┬─────────────────────┐
│ 左侧   │ 右侧商品列表        │
│ 分类   │                   │
│ 导航   │ 2 列宫格          │
│ 80px   │                   │
│        │                   │
│        │                   │
└────────┴─────────────────────┘
```

**左侧分类导航：**
- 宽度：80rpx
- 背景：#F5F5F5
- 选中态：白色背景 + 活力红左边框 3px + 活力红文字
- 条目高度：自适应 padding 15px 10px

**右侧商品列表：**
- 布局：2 列宫格
- 卡片内容：图片 + 名称 + 价格
- 点击跳转：商品详情页

---

### 2.3 搜索模块

#### 2.3.1 搜索页结构

```
┌────────────────────────────────┐
│ 输入框 (搜索图标 + 文字)         │
├────────────────────────────────┤
│ 搜索建议 (实时)                 │
├────────────────────────────────┤
│ 热门搜索 Tag                   │
├────────────────────────────────┤
│ 搜索历史 Tag                  │
└────────────────────────────────┘
```

**输入框规格：**
- 背景：#F0F0F0
- 圆角：22rpx (胶囊)
- placeholder：「搜索好物、品牌、品类」

#### 2.3.2 搜索结果页

```
┌────────────────────────────────┐
│ 输入框 (同步关键词)              │
├────────────────────────────────┤
│ 筛选: 综合 | 价格↑ | 价格↓ | 销量
├────────────────────────────────┤
│ 商品列表                       │
│ (带购物车快捷入口)             │
└────────────────────────────────┘
```

**筛选标签：**
- 选中态：活力红边框 + 活力红文字
- 默认态：灰色边框 + 灰色文字

---

### 2.4 购物车模块

#### 2.4.1 页面结构

```
┌────────────────────────────────┐
│ 商品列表                        │
│ ┌──────────────────────────┐   │
│ │ □ | 图片 | 名称 | ¥XX | + │   │
│ │    |     | 库存 | - | 删除│   │
│ └──────────────────────────┘   │
├────────────────────────────────┤
│ 底部栏                         │
│ □全选  合计 ¥XXX  [结算]     │
└────────────────────────────────┘
```

**购物车项卡片：**
- 选中框：左边
- 商品图：80px × 80px
- 名称：最多 2 行
- 数量编辑器：- 1 + 数字 + +
- 删除：右侧滑动或按钮

**底部结算栏：**
- 固定底部，遮住内容区
- 全选 Checkbox + 合计金额 + 结算按钮
- 结算按钮：活力红背景，白色文字

#### 2.4.2 状态管理

```javascript
// store/cart.js
export const useCartStore = defineStore('cart', {
  state: () => ({
    items: {}, // { [productId]: { qty, checked, skuId } }
  }),
  getters: {
    checkedList: (state) => Object.entries(state.items)
      .filter(([_, v]) => v.checked)
      .map(([pid, v]) => ({ productId: pid, ...v })),
    total: (state) => computed from checkedList * price
  },
  actions: {
    add(productId, skuId, qty) { ... },
    remove(productId) { ... },
    updateQty(productId, qty) { ... },
    toggleChecked(productId, checked) { ... },
    toggleAll(checked) { ... }
  },
  persist: true // 持久化到 storage
});
```

---

### 2.5 订单模块

#### 2.5.1 订单列表页

```
┌────────────────────────────────┐
│ Tab: 全部 | 待付款 | 待发货 | 待收货 | 已完成 | 已取消
├────────────────────────────────┤
│ 订单卡片                        │
│ ┌──────────────────────────┐   │
│ │ 订单号 XXX   待付款      │   │
│ │ 2026-04-14 13:00       │   │
│ │ [商品图 × 2]           │   │
│ │ ¥299           [支付]  │   │
│ └──────────────────────────┘   │
└────────────────────────────────┘
```

**订单卡片结构：**
- 顶部：订单号 + 状态标签（颜色区分）
- 中部：商品缩略图列表
- 底部：金额 + 操作按钮

**状态颜色：**
| 状态 | 颜色 |
|------|------|
| 待付款 | #E4393C (红) |
| 待发货 | #11998E (绿) |
| 待收货 | #E4393C (红) |
| 已完成 | #11998E (绿) |
| 已取消 | #CCCCCC (灰) |

#### 2.5.2 订单详情页

```
┌────────────────────────────────┐
│ 订单信息                       │
│ 订单号: XXX                   │
│ 状态: 待付款                 │
├────────────────────────────────┤
│ 收货信息                     │
│ 张三 138****8888             │
│ 上海市浦东新区张江路123号      │
├────────────────────────────────┤
│ 商品信息                     │
│ [图] 五常大米 10kg    ¥299   │
│ ×2                          │
├────────────────────────────────┤
│ 费用明细                     │
│ 商品小计: ¥598              │
│ 运费: ¥0                    │
│ 应付: ¥598                 │
├────────────────────────────────┤
│ [取消订单]        [立即支付]   │
└────────────────────────────────┘
```

#### 2.5.3 订单状态流转

```
PENDING_PAY ──(支付)──> PENDING_SHIP ──(发货)──> SHIPPED ──(收货)──> FINISHED
     │               │                         │
     └─(取消)───────> CANCELED               │
     └─(超时)───────> CANCELED               │
```

**超时自动取消：** 15 分钟未支付自动取消

---

### 2.6 地址管理模块

#### 2.6.1 地址列表页

```
┌────────────────────────────────┐
│ 地址列表                        │
│ ┌──────────────────────────┐   │
│ │ ● 默认                   │   │
│ │ 张三  138****8888       │   │
│ │ 上海市浦东新区...        │   │
│ │ [选择] [设为默认] [编辑] │   │
│ └──────────────────────────┘   │
│                               │
│ [+ 新增收货地址]              │
└────────────────────────────────┘
```

#### 2.6.2 地址编辑页

```
┌────────────────────────────────┐
│ 新增收货地址                   │
│ ┌──────────────────────────┐   │
│ │ 收货人: [________]     │   │
│ │ 手机号: [________]     │   │
│ │ 省份: [选择▼]       │   │
│ │ 城市: [选择▼]       │   │
│ │ 区/县: [选择▼]     │   │
│ │ 详细地址: [_______] │   │
│ │ □ 设为默认地址      │   │
│ └──────────────────────────┘   │
├────────────────────────────────┤
│ [保存]                        │
└────────────────────────────────┘
```

**字段校验规则：**
| 字段 | 规则 |
|------|------|
| 收货人 | 必填，2-20 字符 |
| 手机号 | 必填，11 位数字 |
| 省份/城市/区 | 必选 |
| 详细地址 | 必填，5-100 字符 |

---

### 2.7 用户模块

#### 2.7.1 个人中心页

```
┌────────────────────────────────┐
│ 头部 (渐变背景)               │
│ ┌────┐                      │
│ │头像│  微信用户           │
│ └────┘                      │
├────────────────────────────────┤
│ 菜单列表                     │
│ 我的订单 →                  │
│ 收货地址 →                 │
└────────────────────────────────┘
```

**微信登录流程：**
1. 调用 `wx.login` 获取 code
2. 换取 openid（唯一标识）
3. （可选）调用 `getUserProfile` 获取昵称头像
4. 存储登录态

**强制登录场景：**
- 下单 / 支付
- 地址管理
- 查看订单

---

### 2.8 商品详情页

```
┌────────────────────────────────┐
│ 商品轮播图                    │ 260px
├────────────────────────────────┤
│ ¥299                          │ 22px, 活力红
│ 商品名称                      │
│ 描述文案...                  │
├────────────────────────────────┤
│ 规格选择                     │
│ [规格A] [规格B]              │
├────────────────────────────────┤
│ 数量                         │
│ [-] 1 [+]  库存: 20        │
├────────────────────────────────┤
│ [加入购物车] [立即购买]       │
└─────────────────���─���────────────┘
```

---

### 2.9 订单确认页

```
┌────────────────────────────────┐
│ 收货地址 (点击选择)            │
│ 张三 138****8888             │
│ 上海市...                    │
├────────────────────────────────┤
│ 商品列表                     │
├────────────────────────────────┤
│ 费用明细                     │
│ 商品小计: ¥598              │
│ 运费: ¥0                   │
│ 优惠: -¥0                  │
│ ─────────────────           │
│ 应付合计: ¥598             │
├────────────────────────────────┤
│ [提交订单 ¥598]             │
└────────────────────────────────┘
```

---

### 2.10 支付模块

#### 2.10.1 微信支付流程

```javascript
// 1. 服务端统一下单
POST /api/v1/pay/create
{
  orderId: 'xxx',
  amount: 59800 // 分
}
// 返回:
// { appId, timeStamp, nonceStr, package, signType, paySign }

// 2. 调起微信支付
uni.requestPayment({
  provider: 'wxpay',
  ...params
})

// 3. 轮询订单状态
GET /api/v1/orders/{orderId}
```

#### 2.10.2 异常处理

- **用户取消：** 保持 PENDING_PAY，可重试
- **支付失败：** 提示重新支付
- **回调未到：** 轮询查询订单状态（1s → 2s → 4s → 最多 30s）
- **支付成功但页面未刷新：** 以订单状态查询接口为准

---

## 3. 组件库设计

### 3.1 基础组件

| 组件名 | 用途 | 规格 |
|--------|------|------|
| `GoodsCard` | 商品卡片 | 3 列 / 2 列 |
| `GoodsImage` | 商品图片 | 1:1, 带加载占位 |
| `PriceText` | 价格显示 | 现价 + 划线价 |
| `TagLabel` | 标签 | 营销标签 |
| `PillButton` | 胶囊按钮 | 选中 / 默认态 |
| `QuantityEditor` | 数量编辑 | +/- 按钮 |
| `AddressCard` | 地址卡片 | 带默认标识 |
| `OrderStatusTag` | 状态标签 | 按状态变色 |
| `LoadingFooter` | 加载更多 | 加载中 / 没有更多 |
| `EmptyState` | 空状态 | 提示 + 可选按钮 |

### 3.2 业务组件

| 组件名 | 路径 | 说明 |
|--------|------|------|
| `HomeBanner` | components/Home/Banner.vue | 首页 Banner 轮播 |
| `HomeCategoryPill` | components/Home/CategoryPill.vue | 分类横滑 |
| `HomeFeatured` | components/Home/Featured.vue | 主推大卡 |
| `HomeGoodsGrid` | components/Home/GoodsGrid.vue | 商品 3 列 |
| `CartItem` | components/Cart/Item.vue | 购物车项 |
| `OrderCard` | components/Order/Card.vue | 订单卡片 |
| `AddressPicker` | components/Address/Picker.vue | 地址选择弹窗 |
| `SkuSelector` | components/Detail/SkuSelector.vue | SKU 选择器 |

---

## 4. API 接口设计

### 4.1 接口域名

```
开发环境: https://api.dev.example.com
测试环境: https://api.test.example.com
生产环境: https://api.example.com
```

### 4.2 接口列表

| 模块 | 方法 | 路径 | 说明 |
|------|------|------|------|
| **区域** | GET | /api/v1/regions | 获取区域列表(启用) |
| **首页** | GET | /api/v1/home/banners | Banner 列表(按 region_code) |
| **首页** | GET | /api/v1/home/featured | 主推商品(按 region_code，可选 category_id) |
| **首页** | GET | /api/v1/home/products | 首页列表商品(按 region_code，可选 category_id，服务端按 N 截取) |
| **分类** | GET | /api/v1/categories | 分类树/列表(启用) |
| **商品** | GET | /api/v1/products | 商品列表(分页/筛选：category_id/keyword/region_code/on_shelf/show_on_home/sort) |
| **商品** | GET | /api/v1/products/{id} | 商品详情(含 SKU) |
| **搜索** | GET | /api/v1/search/hot | 热门搜索 |
| **搜索** | GET | /api/v1/search/suggest | 搜索建议 |
| **搜索** | GET | /api/v1/search/history | 搜索历史(登录) |
| **搜索** | DELETE | /api/v1/search/history | 清空搜索历史(登录) |
| **购物车** | GET | /api/v1/cart | 购物车列表(登录) |
| **购物车** | POST | /api/v1/cart | 添加商品(登录) |
| **购物车** | PUT | /api/v1/cart/{id} | 更新数量(登录) |
| **购物车** | DELETE | /api/v1/cart/{id} | 删除商品(登录) |
| **订单** | POST | /api/v1/orders | 创建订单(登录，需 client_order_token) |
| **订单** | GET | /api/v1/orders | 订单列表(登录) |
| **订单** | GET | /api/v1/orders/{id} | 订单详情(登录) |
| **订单** | PUT | /api/v1/orders/{id}/cancel | 取消订单(登录) |
| **订单** | PUT | /api/v1/orders/{id}/confirm | 确认收货(登录) |
| **订单** | PUT | /api/v1/orders/{id}/pay | 继续支付(登录，订单详情重试支付) |
| **支付** | POST | /api/v1/pay/create | 统一下单(登录) |
| **支付** | GET | /api/v1/pay/status/{order_id} | 支付状态查询(登录) |
| **用户** | POST | /api/v1/user/wx_login | 微信登录 |
| **用户** | GET | /api/v1/user/info | 用户信息 |

### 4.3 请求封装

```javascript
// utils/request.js
const request = (options) => {
  return new Promise((resolve, reject) => {
    const token = storage.get('token');
    uni.request({
      url: API_BASE + options.url,
      method: options.method || 'GET',
      header: {
        'Authorization': token ? `Bearer ${token}` : '',
        'Content-Type': 'application/json'
      },
      data: options.data,
      success: (res) => {
        if (res.statusCode === 200) {
          resolve(res.data);
        } else if (res.statusCode === 401) {
          // token 过期，重新登录
          reject('UNAUTHORIZED');
        } else {
          reject(res.data);
        }
      },
      fail: reject
    });
  });
};
```

#### 4.4 数据源策略（与 PRD/后端联调）

- **服务端为准：** 登录态下，购物车、搜索历史、订单、地址等以服务端数据为准；前端可做本地缓存作为离线兜底与秒开优化，但必须在页面进入/下拉刷新时与服务端同步。
- **本地缓存建议：**
  - `demo_region`：区域选择（始终本地持久化）
  - `token`：登录态（本地持久化，过期需重新登录）
  - `home_cache_{region_code}_{category_id}`：首页数据短缓存（可选，需设置 TTL）
- **登出/Token 失效：** 清理敏感缓存与 store；回到未登录态并提示重新登录。

#### 4.5 端到端接口样例对齐（前端调用与字段映射）

> 本节与后端《技术详细设计说明书 V1.1》`4.14` 保持一致；前端实现以此为准，避免“路径/字段名不一致”导致联调返工。

##### 4.5.1 关键数据结构（前端视角）

```javascript
// Region: 与后端 /regions 对齐
type Region = { code: string; name: string; enabled: boolean; sort?: number };

// Category: 与后端 /categories 对齐（MVP 一级）
type Category = { id: string; name: string; enabled: boolean; sort?: number };

// Home: featured/products 的最小展示字段
type HomeProduct = { id: string; name: string; price: number; original_price?: number; main_image?: string; sales_count?: number; stock?: number };
```

##### 4.5.2 启动与首页首次进入（区域/分类映射 + 首页渲染）

```javascript
// 1) 拉区域（用于 RegionPicker 展示与 code->name 映射）
const regions = await request({ url: '/regions' });
// 2) 拉分类（用于 首页 Pill/分类页；Pill 展示 name，请求使用 id）
const categories = await request({ url: '/categories' });

// 3) 首页数据（region_code 必填；category_id 选填，“全部”不传）
await Promise.all([
  request({ url: '/home/banners', data: { region_code } }),
  request({ url: '/home/featured', data: { region_code, category_id } }),
  request({ url: '/home/products', data: { region_code, category_id } })
]);
```

##### 4.5.3 商品列表/详情（分类 id 传参）

```javascript
// 列表：支持 region_code/category_id/keyword/sort
await request({
  url: '/products',
  data: { region_code, category_id, keyword, page: 1, page_size: 20, sort: 'sales' }
});

// 详情：含 SKU
await request({ url: `/products/${productId}` });
```

##### 4.5.4 下单幂等（client_order_token）+ 支付轮询

```javascript
// 创建订单（幂等 token：一次提交流程复用）
const token = uuidv4();
const order = await request({
  url: '/orders',
  method: 'POST',
  data: {
    region_code,
    address_id,
    client_order_token: token,
    items: [{ product_id, sku_id, quantity }]
  }
});

// 统一下单（拿到 requestPayment 参数）
const payParams = await request({
  url: '/pay/create',
  method: 'POST',
  data: { order_id: order.data.id }
});
await uni.requestPayment({ provider: 'wxpay', ...payParams.data });

// 轮询支付状态（最多 30s，指数退避 1s/2s/4s...）
await request({ url: `/pay/status/${order.data.id}` });
```

##### 4.5.5 常见错误码处理（前端 UX）

- `401`：清理 token，触发重新登录；回到可继续的页面并提示「登录已过期」  
- `400 + message=库存不足`：提示后引导回购物车/详情调整数量，并刷新商品详情/购物车  
- `409`：按后端返回的订单复用结果刷新订单详情（避免重复创建）  

---

## 5. 状态管理设计

### 5.1 Pinia Store 列表

| Store | 用途 | 持久化 |
|-------|------|--------|
| `useUserStore` | 用户信息、登录态 | 是 |
| `useRegionStore` | 当前区域 | 是 |
| `useCartStore` | 购物车 | 是 |
| `useOrderStore` | 订单列表 | 是 |
| `useAddressStore` | 地址列表 | 是 |
| `useSearchStore` | 搜索历史 | 是 |

#### 5.3 订单幂等：client_order_token（前端生成规则）

为满足 PRD 3.3.4 与后端幂等校验，创建订单时前端需要生成并传递 `client_order_token`。

- **生成时机：** 用户点击「提交订单」时生成一次；同一次提交流程（含重复点击/网络重试）复用同 token。
- **生成算法（MVP）：** `uuidv4()`（推荐）或 `timestamp + random`；并缓存到内存变量 `pendingOrderToken`，在订单创建成功/失败后清理。
- **传参：** `POST /api/v1/orders` body 中包含 `client_order_token`。

示例:
```javascript
const token = uuidv4();
await api.orders.create({ addressId, items, client_order_token: token, region_code });
```

### 5.2 持久化封装

```javascript
// store 持久化配置
import { createPinia } from 'pinia';
import persistence from 'pinia-plugin-persistedstate';

const pinia = createPinia();
pinia.use(persistence);

export default pinia;

// storage key 前缀
const PREFIX = 'demo_';
```

---

## 6. 性能优化

### 6.1 图片优化

- 使用 `uni.getImageInfo` 获取尺寸
- 列表首屏图片懒加载
- 使用 CDN + 图像裁剪参数
- WebP 格式优先

### 6.2 列表优化

- 分页加载：每页 20-30 条
- 下拉刷新 + 上拉加载
- 列表数据缓存（按 region + category）
- 长列表虚拟滚动（可选）

### 6.3 接口优化

- 请求拦截器合并重复请求
- 接口数据缓存
- 并发请求合并

---

## 7. 兼容性设计

### 7.1 微信版本兼容

- 最低支持：微信 7.0.12
- 兼容检查：使用 `uni.getSystemInfoSync()`

### 7.2 屏幕适配

```css
/* uni.scss 基础单位 */
page {
  /* 以 375px 屏幕为基准 */
  font-size: 13px;
}

/* 安全区域 */
.safe-area-bottom {
  padding-bottom: constant(safe-area-inset-bottom);
  padding-bottom: env(safe-area-inset-bottom);
}
```

### 7.3 暗黑模式 (预留)

- 方案：CSS 变量 + `prefers-color-scheme`
- MVP 暂不做，默认白名单景

---

## 8. 目录文件清单

### 8.1 页面文件

| 页面 | 文件 |
|------|------|
| 首页 | pages/index/index.vue |
| 分类 | pages/category/category.vue |
| 搜索 | pages/search/search.vue |
| 搜索结果 | pages/search/result.vue |
| 商品详情 | pages/detail/detail.vue |
| 购物车 | pages/cart/cart.vue |
| 确认订单 | pages/confirm/confirm.vue |
| 订单列表 | pages/orders/orders.vue |
| 订单详情 | pages/order-detail/order-detail.vue |
| 我的 | pages/user/user.vue |
| 地址列表 | pages/address/address.vue |
| 地址编辑 | pages/address-edit/address-edit.vue |

### 8.2 组件文件

| 组件 | 路径 |
|------|------|
| 商品卡片 | components/GoodsCard/GoodsCard.vue |
| Banner | components/Banner/Banner.vue |
| 分类Pill | components/CategoryPill/CategoryPill.vue |
| 主推卡 | components/FeaturedCard/FeaturedCard.vue |
| 购物车项 | components/CartItem/CartItem.vue |
| 地址卡 | components/AddressCard/AddressCard.vue |
| 订单卡 | components/OrderCard/OrderCard.vue |
| 区域选择 | components/RegionPicker/RegionPicker.vue |

---

## 9. 验收检查点

### 9.1 首页验收

- [ ] 区域选择器弹窗正常切换
- [ ] Banner 自动轮播 + 手动滑动
- [ ] Banner 点击跳转（商品/分类/搜索）
- [ ] 分类 Pill 横向滚动 + 选中态
- [ ] 主推卡片显示 + 点击跳转
- [ ] 3 列商品列表加载
- [ ] 加载更多功能
- [ ] 加入购物车快捷入口

### 9.2 分类页验收

- [ ] 左侧分类切换
- [ ] 右侧商品列表更新
- [ ] 商品点击跳转详情

### 9.3 搜索验收

- [ ] 实时搜索建议（服务端 `GET /api/v1/search/suggest?keyword=xxx`，前缀匹配，取前 N）
- [ ] 热门搜索标签点击（服务端 `GET /api/v1/search/hot`，基于 `search_history` 聚合）
- [ ] 搜索历史缓存 + 清空（服务端 `GET/DELETE /api/v1/search/history`；本地可缓存但以服务端为准）
- [ ] 结果筛选（综合/价格/销量）
- [ ] 空结果状态

### 9.4 购物车验收

- [ ] 商品勾选 + 全选
- [ ] 数量编辑（库存校验）
- [ ] 删除商品
- [ ] 合计金额计算
- [ ] 库存不足提醒
- [ ] 空购物车状态

### 9.5 订单验收

- [ ] 订单状态筛选
- [ ] 订单卡片显示
- [ ] 订单详情查看
- [ ] 取消订单
- [x] 继续支付（`PUT /api/v1/orders/{id}/pay` 获取 `pay_params`）
- [ ] 确认收货

### 9.6 地址验收

- [ ] 地址列表
- [ ] 新增地址（含校验）
- [ ] 编辑地址
- [ ] 删除地址
- [ ] 设为默认
- [ ] 选择地址下单

### 9.7 支付验收

- [ ] 统一下单
- [ ] 微信支付调起
- [ ] 取消支付处理
- [ ] 支付结果轮询
- [ ] 超时取消订单

---

## 11. 变更记录

- **v1.1**：对齐 PRD v1.3 与后端设计 V1.1：统一 `baseURL=/api/v1`、统一 `region_code/category_id`、拆分首页接口为 banners/featured/products、补充数据源策略与 `client_order_token` 幂等约定，修复文档乱码与明显路径错误。

---

## 10. 待确认事项

1. **接口 Mock**：是否需要本地 Mock 数据？
2. **登录机制**：微信静默登录 vs 强制授权
3. **支付商户号**：申请进度
4. **CDN 图片**：是否配置图像裁剪服务？
5. **性能要求**：首屏加载 ≤ 2s 是否能达成？

---

*End of 前端技术详细设计说明书*